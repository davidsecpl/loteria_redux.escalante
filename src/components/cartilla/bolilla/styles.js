import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container:{
        borderColor:'gray',
        borderWidth: 0.5,
        borderRadius:11,
        marginHorizontal: 8,
        marginVertical:2,
        height: 35,
        width: 50,
        justifyContent: "center"
      },
    label:{
        fontWeight: 'bold',
        textAlign:"center"
    }
});